// 1.create a new array by applying a function to each elemen
// 2.transform the array without changing the original
// 3.it returns a new array with transformed values


const double = [1,2,3].map(num =>num * 2);
console.log(double);
